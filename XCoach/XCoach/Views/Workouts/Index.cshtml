@model IEnumerable<XCoach.Models.WorkoutViewModel.WorkoutIndexViewModel>//IEnumerable<XCoach.Models.Workout>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Workout.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Workout.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.WorkoutType.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Workout.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Workout.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.WorkoutType.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Workout.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Workout.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Workout.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
